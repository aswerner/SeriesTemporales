El script prediccion.py se ejecuta sin necesidad de parámetros.
El script prediccion.py y el fichero Demanda_2015.txt en el que se encuentra el dataset de series temporales que se utiliza en el script deben estar a la misma altura al ejecutar el script.
